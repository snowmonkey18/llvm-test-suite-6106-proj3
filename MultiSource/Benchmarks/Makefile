# MultiSource/Benchmarks Makefile:  Build all subdirectories automatically

LEVEL = ../..

include $(LEVEL)/Makefile.config

# Disabled: OptimizerEval.
PARALLEL_DIRS := Olden MallocBench

include $(LEVEL)/MultiSource/Makefile.multisrc
